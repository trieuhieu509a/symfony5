{% extends 'base.html.twig' %}

{% block title %}
    Hello DefaultController!
{% endblock %}
{% set string = "some strings \n form \n database" %}
{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
    <div class="example-wrapper">
        <h1>Hello {{ controller_name | upper }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a
                            href="{{ '/var/www/html/course/symfony/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ '/var/www/html/course/symfony/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code>
            </li>
        </ul>

        {% set users = [{name: "John"}, {name: "Susan"}, {name: "Robert"}] %}
        {# <ul> #}
        {# {% for user in users %} #}
        {# <li>{{ user.name }} - you won {{ random_gift[loop.index0] }} {{ loop.index0 }}</li> #}
        {# {% endfor %} #}
        {# </ul> #}

        {# {% for message in app.flashes('notice') %} #}
        {# {{ message }} #}
        {# {% endfor %} #}

        {# {% for label,messages in app.flashes %} #}
        {# {% for message in messages %} #}
        {# <div class="flash-{{ label }}"> #}
        {# {{ message }} #}
        {# </div> #}
        {# {% endfor %} #}
        {# {% endfor %} #}

        <ul>
            {% for user in users %}
                <p>{{user.name}}</p>
            {% else %}
                No users where found
            {% endfor %}
        </ul>

        {% for i in 1..10 %}
            <div class="{{ cycle(['even', 'odd'], i) }}">
                {{i}}
            </div>
        {% endfor %}

{#        {{ include('includes/subview.html.twig', {'users': users}) }}#}

        <p>{{ path('home') }}</p>
        <p>{{ url('home') }}</p>
        <p>{{ asset('images/logo.jpg') }}</p>
        <p>{{ absolute_url(asset('images/logo.jpg')) }}</p>
        <img style="width:200px" src="{{ absolute_url(asset('images/logo.jpg')) }}" alt="symfony 4 course">
    </div>

    <p>The google tracking code is {{ ga_code }}</p>

    <h1>{{ app.user }}</h1>
    <h1>{{ app.request.get('param') }}</h1>
    <h1>{{ app.environment }}</h1>
{#    <h1>{{ dump(app.session) }}</h1>#}

    {{ render(controller(
        'App\\Controller\\DefaultController::mostPopularPosts',
        {'number': 5}
    )) }}

{% endblock %}

{#{% block javascripts %}#}
{#    {{ parent() }}#}
{#    <script>#}
{#        console.log('2');#}

{#        var route = "{{ string|escape('js') }}";#}
{#        console.log(route);#}
{#    </script>#}

{#{% endblock %}#}